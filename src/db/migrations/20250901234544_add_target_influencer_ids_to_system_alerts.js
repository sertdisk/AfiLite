exports.up = function(knex) {
  // This is a dummy file to fix a corrupt migration state.
  return Promise.resolve()
}

exports.down = function(knex) {
  // This is a dummy file to fix a corrupt migration state.
  return Promise.resolve()
}
