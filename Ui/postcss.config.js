// Kısa açıklama: PostCSS yapılandırması — Tailwind CSS ve Autoprefixer eklentileri etkin.
/** @type {import('postcss-load-config').Config} */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};